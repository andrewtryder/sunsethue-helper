{
  "name": "functions",
  "description": "Firebase Cloud Functions for Sunsethue Helper",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "test": "node --test test/*.test.js",
    "test:coverage": "c8 --reporter=text --reporter=lcov --check-coverage --lines 80 --functions 80 --branches 75 node --test test/*.test.js"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5",
    "nodemailer": "^8.0.10"
  },
  "devDependencies": {
    "c8": "^10.1.3"
  }
}
